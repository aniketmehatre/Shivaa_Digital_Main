export const environment = {
  production: false,
  youtube: {
    /**
     * NOTE:
     * This is a frontend app, so the API key will be visible in the browser.
     * Restrict it in Google Cloud Console (HTTP referrers) to your domain(s).
     */
    apiKey: '',
    channelId: ''
  }
};


